# `finger`

`finger` is a command-line tool to find out who is who on an HPC cluster.

```bash
finger [username]
```

For example:

```bash
finger svens
```

If `finger` is not installed, these are alternatives with similar
goals:

```bash
pinky [username]
getent passwd [username]
```
